{% extends 'base.html.twig' %}

{% block title %}PriceList index{% endblock %}

{% block body %}
    <h1>PriceList index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for price_list in price_lists %}
            <tr>
                <td>{{ price_list.id }}</td>
                <td>{{ price_list.name }}</td>
                <td>
                    <a href="{{ path('price_list_show', {'id': price_list.id}) }}">show</a>
                    <a href="{{ path('price_list_edit', {'id': price_list.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('price_list_new') }}">Create new</a>
{% endblock %}
