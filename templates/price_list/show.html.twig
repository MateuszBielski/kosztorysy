{% extends 'base.html.twig' %}

{% block title %}PriceList
{% endblock %}

{% block body %}
	<h1>PriceList</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ price_list.id }}</td>
			</tr>
			<tr>
				<th>id z controllera</th>
				<td>{{ id_z_controllera }}</td>
			</tr>
            <tr>
                <td>
                    liczba zapisanych cen {{ price_list.getAmonutOfPrices() }}
                </td>
            </tr>
			{# {% for price in price_list.prices %}
				<tr>
                    <td>{{ price.id }}</td>
                    <td>{{ price.priceValue }}</td>
				</tr>
			{% endfor %} #}
		</tbody>
	</table>

	<a href="{{ path('price_list_index') }}">back to list</a>

	<a href="{{ path('price_list_edit', {'id': price_list.id}) }}">edit</a>

	{{ include('price_list/_delete_form.html.twig') }}
{% endblock %}
