{% extends 'base.html.twig' %}

{% block title %}CostItem{% endblock %}

{% block body %}
    <h1>CostItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cost_item.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cost_item.description }}</td>
            </tr>
            <tr>
                <th>SubDescription</th>
                <td>{{ cost_item.subDescription }}</td>
            </tr>
            <tr>
                <th>MyNumber</th>
                <td>{{ cost_item.myNumber }}</td>
            </tr>
            <tr>
                <th>Unit</th>
                <td>{{ cost_item.unit }}</td>
            </tr>
            <tr>
                <th>Survey</th>
                <td>{{ cost_item.survey }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cost_item_index') }}">back to list</a>

    <a href="{{ path('cost_item_edit', {'id': cost_item.id}) }}">edit</a>

    {{ include('cost_item/_delete_form.html.twig') }}
{% endblock %}
